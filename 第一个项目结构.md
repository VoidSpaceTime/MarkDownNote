# 类
## 代码
```C#
 public record Post : AggregateRootEntity
 {
     public string Title { get; set; } // 标题
     public string Content { get; set; } // 文本内容
     
     public Guid UserId { get; init; } // 用户ID
     public Guid CommentsId { get; set; } // 评论ID
     public List<Comment> Comments { get; set; } = new List<Comment>();
     public int Status { get; set; } // 状态码
     public List<Category> Categories { get; set; } = new List<Category>();

     public string CoverImageUrl { get; set; } // 封面链接
     public List<string> files { get; set; } // 文件列表
 }
```
## 图 不喜欢
``` mermaid
classDiagram 
	AggregateRootEntity <|-- Post 
	AggregateRootEntity <|-- Comment 

	class AggregateRootEntity{
		+Guid Id //ID
		+bool IsDeleted //软删除
		+DateTime CreationTime //创建时间
		+DateTime DeletionTime //删除时间
		+DateTime LastModificationTime //最后修改时间
		+DateTime IHasModificationTime //
		+SoftDelete() //软删除方法
		+NotifyModified() //通知修改
		
	}
	class Post{ 
		+Media Media // 媒体流 可以存放视频或者图片, 视频会多一个播放功能
		+string Title //标题
		+string Context //内容
		+Guid UserId //用户ID
		+List~Commnet~ Comments //评论
		+int Status //文章状态
		+List~Category~ Categorys //种类
	} 
	class Comment{ 
		+bool is_wild 
		+run() 
	}
```
# 流程图
```mermaid
sequenceDiagram
participant user as 用户/前端
participant identity as 用户服务
participant post as 文章服务
participant file as 文件服务

note over identity,file: Commonsinitializer 初始化函数: service注入 MassTransit启用 JWT验证 Cores 跨域 数据库注入

note over identity,file: CommonsDomain 通用领域 通用 实体类 DTO 模型 enum 状态

user ->> + identity: 用户登录并使用服务
identity --> - user: 返回登录状态
identity ->> + post: 用户读取文章列表/文章+评论
post ->> + file: 获取文章附件
file -->> - post: 返回文章附件
post -->> - identity: 返回文章列表/文章+评论
identity ->> + post: 用户提交文章/评论
post ->> + file: 存储文章附件
par 文章验证
	post ->> examiner : 审核
		alt result == 通过 
			post ->> identity : 文章发布 
		else result == 不通过 
			post ->> identity : 文章驳回修改 
		end

	end
post -->> - identity: 返回提交状态


```


---
# 其他测试图标 暂时弃用

```mermaid
      graph TB
        subgraph 直接声明
          direction LR
          普通节点1 --> 普通节点2 --> 普通节点3
        end
        subgraph 完整声明
          普通节点[这是一个普通节点]
          Unicode节点["🐳Unicode节点"]
          Markdown节点["`**第一行**  
          *第二行*  
          ***第三行***`"]
          普通节点 --> Unicode节点 --> Markdown节点
        end
        直接声明 --> 完整声明
```
```mermaid
C4Context
    title 微服务架构 - 系统上下文图
    
    Person(customer, "客户", "使用我们的应用")

    Enterprise_Boundary(b0, "企业边界") {
        System(webApp, "前端应用", "提供用户界面")
    
        SystemDb(userDb, "用户数据库", "存储用户信息")
		SystemDb(postDb, "文章数据库", "存储文章信息")
        SystemDb(commentDb, "评论数据库", "存储评论信息")
        
        System_Boundary(microservices, "微服务系统") {
            System(apiGateway, "API网关", "请求路由和负载均衡")
            System(authService, "认证服务", "处理用户认证")
            System(userService, "用户服务", "用户管理")
        }

        System_Boundary(postServices, "文章系统") {
            System(postService, "文章服务", "文章发布/获取")
            System(commentService, "评论服务", "评论发布/获取")
        }
    }
    
	Rel(customer, webApp, "使用")
    Rel(webApp, apiGateway, "API请求")
    Rel(apiGateway, authService, "路由认证请求")
    Rel(apiGateway, userService, "路由用户请求")
    Rel(userService, userDb, "用户信息验证")
```
